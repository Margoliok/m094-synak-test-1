# М094 — Сынақ тесті №1

Дұрыс жауаптар мен түсіндірмелер.

---

## 1-СПЕЦ. Бөлім: Алгоритмдер және деректер құрылымы (30 сұрақ)

### Сұрақ 1. Алгоритмдердің міндетті қасиеттерінің бірі
- A) түсіндірмелік
- B) анықталмағандық
- C) шексіздік
- **✅ D) дискреттілік**
- E) үздіксіздік

**Түсіндірме:** Алгоритмнің міндетті қасиеттері — дискреттілік (қадамдарға бөліну), анықталғандық, нәтижелілік, жалпылық, шектілік. «Дискреттілік» осы тізімде.

---

### Сұрақ 2. Көптік таңдау құрылымы
- A) if
- B) do-while
- C) while-do
- D) if-else
- **✅ E) switch**

**Түсіндірме:** `switch` — көптік таңдау (multi-way selection) операторы, бір мәнді бірнеше нұсқамен салыстырады. `if/if-else` — қарапайым таңдау, қалғандары — цикл.

---

### Сұрақ 3. Экранға шығатын жауап
```cpp
double x=3.5;
int y;
if(x==0) y=7*pow(x,3);
else y=2*pow(x,3);
cout<<y;
```
- A) 0
- B) 85.75
- **✅ C) 85**
- D) 75
- E) 54

**Түсіндірме:** `x==0` жалған, сондықтан `y=2*pow(3.5,3)=2*42.875=85.75`. Бірақ `y` — `int`, бөлшек бөлігі қиылып, **85** шығады.

---

### Сұрақ 4. Префиксті декремент дұрыс жазылуы
- A) a--
- B) -=a
- C) a-=
- **✅ D) --a**
- E) +=a

**Түсіндірме:** Префиксті декремент — операнд алдында тұрады: `--a`. Мән алдымен азаяды, содан кейін қолданылады.

---

### Сұрақ 5. Постфиксті декремент дұрыс жазылуы
- A) -=a
- B) --a
- C) a++
- D) +=a
- **✅ E) a--**

**Түсіндірме:** Постфиксті декремент — операндтан кейін тұрады: `a--`. Мән алдымен қолданылып, содан кейін азаяды.

---

### Сұрақ 6. ЖӘНЕ логикалық операторының дұрыс жазылуы
- A) a||b
- **✅ B) a&&b**
- C) a!
- D) a//b
- E) a<<b

**Түсіндірме:** C++ тілінде логикалық ЖӘНЕ — `&&`. `||` — НЕМЕСЕ, `!` — ЕМЕС, `<<` — жылжыту.

---

### Сұрақ 7. S жолы бос болғанда true, бос болмағанда false қайтаратын әдіс
- A) S.substr()
- B) S.find()
- C) S.insert()
- D) S.size()
- **✅ E) S.empty()**

**Түсіндірме:** `empty()` жол бос болса `true`, әйтпесе `false` қайтарады. `size()` сан қайтарады.

---

### Сұрақ 8. Жауапқа шығатын өрнек
```cpp
int i = 25;
if (i > 15) cout << "i-дің мәні 15 -тен үлкен";
else cout << "i-дің мәні 15-тен кіші";
```
- **✅ A) i-дің мәні 15 -тен үлкен**
- B) i-дің мәні 15-ке жуық
- C) i-дің мәні 15-ке тең
- D) i-дің мәні белгісіз
- E) i-дің мәні 15-тен кіші

**Түсіндірме:** `25 > 15` шарты ақиқат, сондықтан бірінші тармақ орындалады.

---

### Сұрақ 9. Массивтерді сыртқы сұрыптау алгоритмі
- **✅ A) біріктіріп сұрыптау**
- B) қосып сұрыптау
- C) шейкерлік сұрыптау
- D) көпіршікті сұрыптау
- E) кірістіріп сұрыптау

**Түсіндірме:** Сыртқы сұрыптау (жады сыймайтын үлкен деректер, файлдар) негізінен **біріктіріп сұрыптауға (merge sort)** негізделеді.

---

### Сұрақ 10. int func() функциясы қайтарады
- A) символ деректі
- B) мәтін деректі
- **✅ C) бүтін деректі**
- D) бөлшек деректі
- E) логикалық деректі

**Түсіндірме:** `int` қайтару типі — бүтін сан.

---

### Сұрақ 11. f(n)=n! функциясының дұрыс берілуі
- A) f(0)=1, f(n)=f(n-1)+1
- B) f(0)=0, f(n)=f(n-1)+2
- C) f(0)=1, f(n)=2n·f(n-1)
- **✅ D) f(0)=1, f(n)=n·f(n-1)**
- E) f(0)=0, f(n)=f(n-1)+1

**Түсіндірме:** Факториалдың рекурсивті анықтамасы: `0! = 1`, `n! = n·(n-1)!`.

---

### Сұрақ 12. printf() функциясының дұрыс жазылуы
- A) int a; printf("%c", a);
- B) int a; printf("a=%s", a);
- C) int a; printf("%s", a);
- **✅ D) int a; printf("%d", a);**
- E) int a; printf("a=&i", a);

**Түсіндірме:** Бүтін санды шығару спецификаторы — `%d`. `%c` — символ, `%s` — жол.

---

### Сұрақ 13. Массивтегі іздеу алгоритмі
- A) шелл іздеуі
- B) таңдап іздеу
- C) кірістіріп іздеу
- **✅ D) екілік (бинарлы) іздеу**
- E) квадраттық іздеу

**Түсіндірме:** Іздеу алгоритмі — **екілік (бинарлы) іздеу**. Қалғандары (шелл, таңдап, кірістіріп) — сұрыптау әдістері.

---

### Сұрақ 14. 2 3 17 7 8 9 1 4 6 9 2 3 1 18 үшін табиғи біріктіріп сұрыптаудың бірінші этапы
- **✅ A) b=2 3 17 1 4 6 9 1 18   c=7 8 9 2 3   a=2 3 7 8 9 17 1 2 3 4 6 9 1 18**
- B) …
- C) …
- D) …
- E) …

**Түсіндірме:** Табиғи сериялар (өспелі тізбектер): `[2 3 17][7 8 9][1 4 6 9][2 3][1 18]`. Кезектесіп бөлеміз: b-ға 1,3,5-ші серия → `2 3 17 | 1 4 6 9 | 1 18`, c-ға 2,4-ші серия → `7 8 9 | 2 3`. Біріктіргенде a = `2 3 7 8 9 17 | 1 2 3 4 6 9 | 1 18`.

---

### Сұрақ 15. (6,3,2,8,1,7,4,5) — көпіршік әдісінің бірінші қадамының нәтижесі
- A) (1,2,3,8,6,7,4,5)
- B) (3,2,8,6,7,4,5,1)
- **✅ C) (3,2,6,1,7,4,5,8)**
- D) (2,6,3,1,4,8,5,7)
- E) (6,3,2,8,1,7,4,5)

**Түсіндірме:** Бір өтуде көрші элементтер салыстырылып, ең үлкені (8) соңына «қалқып» шығады: `6,3,2,8,1,7,4,5 → 3,2,6,1,7,4,5,8`.

---

### Сұрақ 16. Көпіршікті сұрыптаудың уақыттық күрделілігі
- A) O(n³)
- B) O(n·log n)
- **✅ C) O(n²)**
- D) O(1)
- E) O(log n)

**Түсіндірме:** Көпіршікті сұрыптау — қос кірістірілген цикл, ең нашар/орташа жағдайда **O(n²)**.

---

### Сұрақ 17. Алгоритм күрделілігінің өсу ретімен орналасқан нұсқа
- A) O(N²), O(N), O(log N)
- **✅ B) O(1), O(N), O(N³)**
- C) O(2N), O(N), O(1)
- D) O(N), O(1), O(N·log N)
- E) O(N²), O(log N), O(N³)

**Түсіндірме:** Өсу реті: `O(1) < O(log N) < O(N) < O(N·log N) < O(N²) < O(N³)`. Тек B нұсқасы толығымен өспелі.

---

### Сұрақ 18. O(log n) уақытта жұмыс істейтін алгоритм
- A) екі матрицаны көбейту
- B) сызықты іздеу
- C) көпіршікті сұрыптау
- D) бүтін санның тақ-жұптығын табу
- **✅ E) екілік іздеу алгоритмі**

**Түсіндірме:** Екілік (бинарлы) іздеу әр қадамда аралықты екіге бөледі → **O(log n)**. (Тақ-жұптықты табу — O(1), сызықты іздеу — O(n).)

---

### Сұрақ 19. Бір байланыстырылған тізім анықтамасы
- A) FILO принципі бойынша жұмыс істейтін құрылым
- B) келесі және алдыңғы түйінге көрсеткіші бар құрылым
- C) LILO принципі бойынша жұмыс істейтін құрылым
- D) LIFO принципі бойынша жұмыс істейтін құрылым
- **✅ E) дерек пен келесі түйінге көрсеткіші бар мәліметтер құрылымы**

**Түсіндірме:** Бір байланыстырылған тізімде әр түйін деректен және **тек келесі** түйінге сілтемеден тұрады. Алдыңғы мен келесіге сілтеме — қос байланыстырылған тізім.

---

### Сұрақ 20. C++ тілінде динамикалық бүтін типті массивті жариялау
- **✅ A) int* a = new int[n]**
- B) int* a = new int*[n]
- C) int a = new int[n]
- D) int a[n]
- E) int* a = new int(n)

**Түсіндірме:** Динамикалық массив: `int* a = new int[n];` — n элементті бүтін массив. `new int(n)` — бір мәні n болатын элемент, `new int*[n]` — көрсеткіштер массиві.

---

### Сұрақ 21. Стек әдістеріне жатпайтын функция
- A) pop()
- B) size()
- C) push()
- D) clear
- **✅ E) contains()**

**Түсіндірме:** Стектің негізгі операциялары — `push()`, `pop()`, `top()`, `size()`, `empty()`. `contains()` (элементті іздеу) стек интерфейсіне жатпайды.

---

### Сұрақ 22. Хеш кестесінің анықтамасы
- A) типтері бірдей элементтерден тұратын құрылым
- B) әр түрлі типті реттелген деректер құрылымы
- C) FIFO принципі бойынша жұмыс істейтін құрылым
- **✅ D) өте жылдам іздеу мен қоюды қамтамасыздандыратын деректер құрылымы**
- E) бір-бірімен байланысқан деректер құрылымы

**Түсіндірме:** Хеш-кесте кілт → индекс есептеу арқылы орташа **O(1)** уақытта жылдам іздеу мен қоюды қамтамасыз етеді.

---

### Сұрақ 23. Толтырылған массив ұяшығына кілтті хештеудің атауы
- A) бірігу
- **✅ B) коллизия**
- C) ажырау
- D) қиылысу
- E) бөліну

**Түсіндірме:** Екі түрлі кілт бір ұяшыққа хештелсе — бұл **коллизия**.

---

### Сұрақ 24. Хеш кестесінде коллизиядан құтылу әдісі
- **✅ A) ашық адресациялау әдісі**
- B) трапеция әдісі
- C) орамдар әдісі
- D) қадамдық бөлшектеу әдісі
- E) квадрат ортасы әдісі

**Түсіндірме:** Коллизияны шешу әдістері — **ашық адресациялау** (open addressing) және тізбектеу (chaining). Қалғандары — коллизия шешу әдістері емес.

---

### Сұрақ 25. AVL ағашында жою операциясының орындалу уақыты
- A) O(n³)
- B) O(n²)
- C) O(n·log n)
- **✅ D) O(log n)**
- E) O(1)

**Түсіндірме:** AVL — теңгерілген ағаш, биіктігі O(log n). Іздеу, қою, жою — барлығы **O(log n)**.

---

### Сұрақ 26. 20 түйінді толық екілік ағаш, тамыры 0-деңгейде — 4-деңгейдегі түйін саны
- A) 0
- B) 8
- C) 1
- D) 4
- **✅ E) 5**

**Түсіндірме:** Деңгейлердегі түйін саны: 0→1, 1→2, 2→4, 3→8. Барлығы 15 түйін. Қалғаны `20−15 = 5` түйін 4-деңгейде орналасады.

---

### Сұрақ 27. Қызылды-қаралы ағаш қасиеттерінің бірі
- A) сол ішкі ағаштың кілті түйін кілтінен үлкен болса
- B) барлық жапырақтар бір деңгейде орналасса
- C) сол ішкі ағаштың кілті түйін кілтінен кіші болса
- **✅ D) егер төбе қызыл болса, оның екі мұрагері де қара**
- E) әрбір түйіннің ең болмағанда бір ішкі ағашы бос

**Түсіндірме:** Қызылды-қаралы ағаштың негізгі қасиеті: **қызыл түйіннің балалары міндетті түрде қара**. (C — жалпы екілік іздеу ағашының қасиеті.)

---

### Сұрақ 28. Графтың түйінінің атауы
- A) мекенжай
- B) қабырға
- **✅ C) төбе**
- D) нүкте
- E) жол

**Түсіндірме:** Граф түйіні = **төбе (vertex)**. Түйіндерді қосатын — қабырға (edge).

---

### Сұрақ 29. Барлық төбелік жұптар үшін ең қысқа жолдарды құру есебі
- A) бастапқы төбеден бірнеше қабырға қашықтықтағы төбелерге дейін
- **✅ B) әр төбеден барлық басқа төбелерге дейін**
- C) бастапқы төбеден бір қабырға қашықтықтағы төбелерге дейін
- D) бастапқы төбеден басқа төбелерге дейін
- E) әр төбеден бастапқы төбеге дейін

**Түсіндірме:** «Барлық жұптар» есебінде (all-pairs shortest path, мыс. Флойд–Уоршелл) **әр төбеден барлық басқа төбелерге дейінгі** ең қысқа жол ізделеді.

---

### Сұрақ 30. Әр төбесі басқаларымен қабырғалармен жалғанған граф
- A) толық емес граф
- B) гиперграф
- C) шынжыр (тізбек)
- **✅ D) толық граф**
- E) мультиграф

**Түсіндірме:** Әр төбе қалған барлық төбелермен қабырғамен жалғанса — бұл **толық граф (complete graph)**.

---

## 2-СПЕЦ ПӘН. Бөлім: Деректер базасы (20 сұрақ)

> *Бір немесе бірнеше дұрыс жауабы бар (үш дұрыс жауаптан артық емес).*

### Сұрақ 1. Факультет–Кафедра–Оқытушы байланысын бейнелейтін ағаштар тізімінен тұратын деректер моделі
- **✅ B) иерархиялық**

**Түсіндірме:** Ағаш түріндегі (ата-ана → бала) байланыс — **иерархиялық** деректер моделі.

---

### Сұрақ 2. Біріктіру (union) амалының нәтижесі
- **✅ B) бірінші және екінші қатынасқа кіретін барлық кортеждер жиыны**
- **✅ F) кемінде бір қатынастағы барлық кортеждер жиыны**

**Түсіндірме:** Біріктіру (∪) — екі қатынастың **кемінде біреуінде** кездесетін барлық кортеждерді береді. (A — қиылысу, C — айырма.)

---

### Сұрақ 3. Жиыннан элементтерді тек берілген шарт бойынша іріктеп алу амалы
- **✅ E) таңдау**

**Түсіндірме:** **Таңдау (selection, σ)** — шартқа сәйкес жолдарды (кортеждерді) іріктейді. Проекция — бағандарды таңдайды.

---

### Сұрақ 4. Жалғыз мәнді атрибут
- **✅ B) адам жасы**
- **✅ E) факультет деканы**

**Түсіндірме:** Жалғыз мәнді (single-valued) атрибуттың бір ғана мәні болады: жас — бір сан, деканы — біреу. Тізімдер (тауарлар, студенттер, дипломдар, телефондар) — көп мәнді.

---

### Сұрақ 5. Сыртқы кілтті құру шарты
- **✅ A) байланысқан кестенің бастапқы кілтінің типіне сай болу**
- **✅ F) байланысқан кестенің әлеуетті кілтіне тәуелді болады**

**Түсіндірме:** Сыртқы кілт байланысқан кестенің бастапқы/әлеуетті кілтіне сілтейді және онымен **типі бойынша сәйкес** болуы керек.

---

### Сұрақ 6. Кітаптың аты мен авторлар тізімі (авторлар массивте)
- **✅ A)** `SELECT B.Title, A.Author FROM BOOKS AS B, UNNEST(B.AuthArray) AS A(Author);`

**Түсіндірме:** `UNNEST` — массивті (AuthArray) жеке жолдарға жаяды, әр кітап–автор жұбын шығарады.

---

### Сұрақ 7. Биология кафедрасының оқытушылары
- **✅ D)** `SELECT Name FROM INSTRUCTOR WHERE DeptName = 'Биология';`

**Түсіндірме:** Дұрыс сүзгі — `WHERE DeptName = 'Биология'`. (`<>` — тең емес, `GROUP BY` — топтау, орынсыз.)

---

### Сұрақ 8. «LIKE 'ab\%cd%'» түрінде берілген болса
- **✅ D) "ab%cd" тіркесінен басталады**

**Түсіндірме:** `\%` — экрандалған, әдеттегі **%** таңбасы (литерал). Соңғы `%` — кез келген символдар. Демек жол **"ab%cd"** тіркесінен басталады.

---

### Сұрақ 9. Бөлім бойынша, аты-жөні алфавитпен сұрыпталатын және жұмыс проектісі
- **✅ A)**
```sql
SELECT D.Dname, E.Lname, E.Fname, P.Pname
FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
WHERE D.Dnum = E.Dno AND E.Ssn = W.Essn AND W.Pno = P.Pnum
ORDER BY D.Dname, E.Lname, E.Fname;
```

**Түсіндірме:** Кестелерді дұрыс байланыстыратын `WHERE` шарттары **және** сұрыптау үшін `ORDER BY` бар жалғыз нұсқа — A. (E-де `ORDER BY` жоқ.)

---

### Сұрақ 10. Сұраныстың нәтижесі
```sql
SELECT Semester, COUNT(*) AS '60-тан көп сағаттар саны'
FROM UCHPLAN WHERE Hours>60 GROUP BY ALL Semester
```
- **✅ D) Әр семестрдегі сағат саны 60-тан көп болатын сабақтар санын шығарады**

**Түсіндірме:** `WHERE Hours>60` сүзгісі + `GROUP BY Semester` → әр семестр бойынша сағаты 60-тан асатын сабақтардың санын береді.

---

### Сұрақ 11. Әр проект үшін номері, аты және онда істеген жұмыскерлер саны
- **✅ A)**
```sql
SELECT Pnum, Pname, COUNT(*)
FROM PROJECT, WORKS_ON
WHERE Pnum=Pno
```

**Түсіндірме:** Проекті мен жұмысшыларды `Pnum=Pno` арқылы байланыстырып, `COUNT(*)` санайды. (B — `HAVING`-пен шектеу қосады, C/D — синтаксис қатесі.)

---

### Сұрақ 12. Негізгі кестедегі жол өшірілсе, байланысқан жол да жойылатын кілттік сөз
- **✅ H) ON DELETE CASCADE**

**Түсіндірме:** `ON DELETE CASCADE` — ата-жол жойылғанда, оған тәуелді жолдар автоматты түрде өшіріледі.

---

### Сұрақ 13. Стипендиясы 25000 болатындарды екі есе көбейту
- **✅ E)**
```sql
UPDATE STUDENTS
SET STIP = STIP * 2
WHERE STIP = 25000;
```

**Түсіндірме:** Бар жолдарды өзгерту — `UPDATE ... SET ... WHERE`. (`CREATE/INSERT/DELETE` орынсыз.)

---

### Сұрақ 14. 2003 кодты сабақтан барлық студенттің балын NULL-ға өзгерту
- **✅ D)**
```sql
UPDATE USP
SET Ocenka = NULL
WHERE Pnum = 2003;
```

**Түсіндірме:** Мәнді NULL-ге өзгерту — `UPDATE ... SET Ocenka = NULL WHERE Pnum = 2003`.

---

### Сұрақ 15. UNIQUE қолданып, 2021 жылы бір рет қана оқылған пәндер
- **✅ D)**
```sql
SELECT T.CourseId
FROM COURSE AS T
WHERE UNIQUE (SELECT R.CourseId
              FROM SECTION AS R
              WHERE T.CourseId = R.CourseId AND R.Year = 2021);
```

**Түсіндірме:** `UNIQUE` ішкі сұраныс нәтижесінде қайталанатын жол болмаса `true` қайтарады — яғни пән 2021 жылы бір-ақ рет оқылған. Корреляция (`T.CourseId = R.CourseId`) және `R.Year = 2021` дұрыс.

---

### Сұрақ 16. 2021 жылы оқылған пәндер (COUNT арқылы)
- **✅ A)**
```sql
SELECT T.CourseId
FROM COURSE AS T
WHERE 1 <= (SELECT COUNT(R.CourseId)
            FROM SECTION AS R
            WHERE T.CourseId = R.CourseId AND R.Year = 2021);
```

**Түсіндірме:** Ішкі сұраныста корреляция да (`T.CourseId = R.CourseId`), жыл сүзгісі де (`R.Year = 2021`) дұрыс орналасқан жалғыз нұсқа — A.

---

### Сұрақ 17. Философия кодынан 1 санға кем сабақ туралы ақпарат
- **✅ A)**
```sql
SELECT * FROM PREDMET
WHERE Pnum = (SELECT Pnum-1 FROM PREDMET WHERE Pname = 'Философия');
```

**Түсіндірме:** Ішкі сұраныс Философияның кодынан 1 кемін (`Pnum-1`) есептейді, сыртқы сұраныс сол кодты жазбаны табады.

---

### Сұрақ 18. 1НФ (бірінші қалыпты форма)
- **✅ C) логикалық бөлінбейтін мәндерге ие қатынас**

**Түсіндірме:** 1НФ — барлық атрибут мәндері **атомарлы (әрі қарай бөлінбейтін)** болуы шарт.

---

### Сұрақ 19. Скалярлық қатынас
- **✅ D) логикалық бөлінбейтін мәндерге ие қатынас**

**Түсіндірме:** Скалярлық (атомарлы) қатынас — әр ұяшықта бір ғана **бөлінбейтін** мән болады.

---

### Сұрақ 20. Денормалдауда пайда болатын аномалия
- **✅ B) өзгерту аномалиясы**
- **✅ D) енгізу аномалиясы**
- **✅ F) жою аномалиясы**

**Түсіндірме:** Классикалық үш аномалия: **енгізу (insert)**, **өзгерту/жаңарту (update)**, **жою (delete)**. Қалғандары (іздеу, сұрыптау, байланыс аномалиясы) — жоқ ұғымдар.

---

*Ескерту: тест жауаптары стандартты алгоритм/ДББ теориясы бойынша анықталды. Кейбір «бірнеше дұрыс жауабы бар» сұрақтарда (2-бөлім, №2) тұжырымдама екіұшты болуы мүмкін.*
