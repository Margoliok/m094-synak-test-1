# М094 — Сынақ тесті №1

Барлық сұрақтар, нұсқалар, дұрыс жауаптар және түсіндірмелер.

**Нөмірлеу.** Тақырып жолындағы нөмір — тесттегі сквоздық нөмір (1–50), ол интерактивті
нұсқадағы «Сұрақ №N» дегенмен дәл сәйкес келеді. Жақшада тесттің өз бөлімдік нөмірі
көрсетілген (мыс. `2-бөлім №1`).

**⚠ таңбасы** — тесттің өз тұжырымындағы кемшілік: шешімде емес, сұрақтың немесе
нұсқалардың өзінде қате/екіұштылық бар деген белгі.

---

## 1-СПЕЦ. Бөлім: Алгоритмдер және деректер құрылымы (1–30)

> *Бір дұрыс жауап.*

### Сұрақ 1 (1-бөлім №1). Алгоритмдердің міндетті қасиеттерінің бірі
- A) түсіндірмелік
- B) анықталмағандық
- C) шексіздік
- **✅ D) дискреттілік**
- E) үздіксіздік

**Түсіндірме:** Алгоритмнің міндетті қасиеттері — дискреттілік (қадамдарға бөліну), анықталғандық, нәтижелілік, жалпылық, шектілік.

---

### Сұрақ 2 (1-бөлім №2). Көптік таңдау құрылымы
- A) if
- B) do-while
- C) while-do
- D) if-else
- **✅ E) switch**

**Түсіндірме:** `switch` — көптік таңдау (multi-way selection) операторы. `if/if-else` — қарапайым таңдау, қалғандары — цикл.

---

### Сұрақ 3 (1-бөлім №3). Экранға шығатын жауап
```cpp
double x=3.5;
int y;
if(x==0) y=7*pow(x,3);
else y=2*pow(x,3);
cout<<y;
```
- A) 0
- B) 85.75
- **✅ C) 85**
- D) 75
- E) 54

**Түсіндірме:** `x==0` жалған, сондықтан `y=2*pow(3.5,3)=2*42.875=85.75`. Бірақ `y` — `int`, бөлшек бөлігі қиылып, **85** шығады.

---

### Сұрақ 4 (1-бөлім №4). Префиксті декремент дұрыс жазылуы
- A) a--
- B) -=a
- C) a-=
- **✅ D) --a**
- E) +=a

**Түсіндірме:** Префиксті декремент операнд алдында тұрады: `--a`. Мән алдымен азаяды, содан кейін қолданылады.

---

### Сұрақ 5 (1-бөлім №5). Постфиксті декремент дұрыс жазылуы
- A) -=a
- B) --a
- C) a++
- D) +=a
- **✅ E) a--**

**Түсіндірме:** Постфиксті декремент операндтан кейін тұрады: `a--`. Мән алдымен қолданылып, содан кейін азаяды.

---

### Сұрақ 6 (1-бөлім №6). ЖӘНЕ логикалық операторының дұрыс жазылуы
- A) a||b
- **✅ B) a&&b**
- C) a!
- D) a//b
- E) a<<b

**Түсіндірме:** C++ тілінде логикалық ЖӘНЕ — `&&`. `||` — НЕМЕСЕ, `!` — ЕМЕС, `<<` — жылжыту.

---

### Сұрақ 7 (1-бөлім №7). S жолы бос болғанда true, бос болмағанда false қайтаратын әдіс
- A) S.substr()
- B) S.find()
- C) S.insert()
- D) S.size()
- **✅ E) S.empty()**

**Түсіндірме:** `empty()` жол бос болса `true`, әйтпесе `false` қайтарады. `size()` сан қайтарады.

---

### Сұрақ 8 (1-бөлім №8). Жауапқа шығатын өрнек
```cpp
int i = 25;
if (i > 15) cout << "i-дің мәні 15 -тен үлкен";
else cout << "i-дің мәні 15-тен кіші";
```
- **✅ A) i-дің мәні 15 -тен үлкен**
- B) i-дің мәні 15-ке жуық
- C) i-дің мәні 15-ке тең
- D) i-дің мәні белгісіз
- E) i-дің мәні 15-тен кіші

**Түсіндірме:** `25 > 15` шарты ақиқат, сондықтан бірінші тармақ орындалады.

---

### Сұрақ 9 (1-бөлім №9). Массивтерді сыртқы сұрыптау алгоритмі
- **✅ A) біріктіріп сұрыптау**
- B) қосып сұрыптау
- C) шейкерлік сұрыптау
- D) көпіршікті сұрыптау
- E) кірістіріп сұрыптау

**Түсіндірме:** Сыртқы сұрыптау (жады сыймайтын үлкен деректер, файлдар) негізінен **біріктіріп сұрыптауға (merge sort)** негізделеді.

---

### Сұрақ 10 (1-бөлім №10). int func() функциясы қайтарады
- A) символ деректі
- B) мәтін деректі
- **✅ C) бүтін деректі**
- D) бөлшек деректі
- E) логикалық деректі

**Түсіндірме:** `int` қайтару типі — бүтін сан.

---

### Сұрақ 11 (1-бөлім №11). f(n)=n! функциясының дұрыс берілуі
- A) f(0)=1, f(n)=f(n-1)+1
- B) f(0)=0, f(n)=f(n-1)+2
- C) f(0)=1, f(n)=2n·f(n-1)
- **✅ D) f(0)=1, f(n)=n·f(n-1)**
- E) f(0)=0, f(n)=f(n-1)+1

**Түсіндірме:** Факториалдың рекурсивті анықтамасы: `0! = 1`, `n! = n·(n-1)!`.

---

### Сұрақ 12 (1-бөлім №12). printf() функциясының дұрыс жазылуы
- A) int a; printf("%c", a);
- B) int a; printf("a=%s", a);
- C) int a; printf("%s", a);
- **✅ D) int a; printf("%d", a);**
- E) int a; printf("a=&i", a);

**Түсіндірме:** Бүтін санды шығару спецификаторы — `%d`. `%c` — символ, `%s` — жол.

---

### Сұрақ 13 (1-бөлім №13). Массивтегі іздеу алгоритмінің классификациясы
- A) шелл іздеуі
- B) таңдап іздеу
- C) кірістіріп іздеу
- **✅ D) екілік (бинарлы) іздеу**
- E) квадраттық іздеу

**Түсіндірме:** Жалғыз нақты іздеу алгоритмі — **екілік (бинарлы) іздеу**. Қалған нұсқалар сұрыптау әдістерінің атауларынан («шелл», «таңдап», «кірістіріп» сұрыптау) жасалған жалған терминдер.

---

### Сұрақ 14 (1-бөлім №14). 2 3 17 7 8 9 1 4 6 9 2 3 1 18 тізбегі үшін табиғи біріктіріп сұрыптаудың бірінші этапы
- **✅ A) b=2 3 17 1 4 6 9 1 18   c=7 8 9 2 3   a= 2 3 7 8 9 17 1 2 3 4 6 9 1 18**
- B) b=21 2 3 3 4 6 7 8 9 9 17   c=18 1   a= 1 1 2 2 3 3 4 6 7 8 9 9 17 18
- C) b=1 2 2 3 3 4 6 7 8 9 9 17   c=1 18   a= 1 18 2 2 3 3 4 6 7 8 9 9 17 1
- D) b=2 3 17 7 4 6 9 1 18   c= 1 8 9 2 3   a= 1 2 2 3 3 4 6 7 8 9 9 17 1 18
- E) b=2 3 7 8 9 17 1 18   c=1 2 3 4 6 9   a= 2 3 7 8 9 17 1 2 3 4 6 9 1 18

**Түсіндірме:** Табиғи сериялар (өспелі тізбектер): `[2 3 17][7 8 9][1 4 6 9][2 3][1 18]`. Кезектесіп бөлеміз: b-ға 1, 3, 5-ші серия → `2 3 17 | 1 4 6 9 | 1 18`, c-ға 2, 4-ші серия → `7 8 9 | 2 3`. Біріктіргенде a = `2 3 7 8 9 17 | 1 2 3 4 6 9 | 1 18`.

---

### Сұрақ 15 (1-бөлім №15). (6, 3, 2, 8, 1, 7, 4, 5) — көпіршік әдісінің бірінші қадамының нәтижесі
- A) (1, 2, 3, 8, 6, 7, 4, 5)
- B) (3, 2, 8, 6, 7, 4, 5, 1)
- **✅ C) (3, 2, 6, 1, 7, 4, 5, 8)**
- D) (2, 6, 3, 1, 4, 8, 5, 7)
- E) (6, 3, 2, 8, 1, 7, 4, 5)

**Түсіндірме:** Бір өтуде көрші элементтер салыстырылып, ең үлкені (8) соңына «қалқып» шығады: `6,3,2,8,1,7,4,5 → 3,2,6,1,7,4,5,8`.

---

### Сұрақ 16 (1-бөлім №16). Көпіршікті сұрыптау алгоритмінің уақыттық күрделілігі
- A) O(n³)
- B) O(n·log n)
- **✅ C) O(n²)**
- D) O(1)
- E) O(log n)

**Түсіндірме:** Көпіршікті сұрыптау — қос кірістірілген цикл, ең нашар/орташа жағдайда **O(n²)**.

---

### Сұрақ 17 (1-бөлім №17). Алгоритм күрделілігінің өсу ретімен орналасқан нұсқалары
- A) O(N²), O(N), O(log N)
- **✅ B) O(1), O(N), O(N³)**
- C) O(2N), O(N), O(1)
- D) O(N), O(1), O(N·log N)
- E) O(N²), O(log N), O(N³)

**Түсіндірме:** Өсу реті: `O(1) < O(log N) < O(N) < O(N·log N) < O(N²) < O(N³)`. Тек B нұсқасы толығымен өспелі.

---

### Сұрақ 18 (1-бөлім №18). O(log n) уақытта жұмыс істейтін алгоритм
- A) екі матрицаны көбейту
- B) сызықты іздеу
- C) көпіршікті сұрыптау
- D) бүтін санның тақ-жұптығын табу
- **✅ E) екілік іздеу алгоритмі**

**Түсіндірме:** Екілік (бинарлы) іздеу әр қадамда аралықты екіге бөледі → **O(log n)**. (Тақ-жұптықты табу — O(1), сызықты іздеу — O(n).)

---

### Сұрақ 19 (1-бөлім №19). Бір байланыстырылған тізім анықтамасы
- A) FILO принципі бойынша жұмыс істейтін құрылым
- B) келесі және алдыңғы түйінге көрсеткіші бар құрылым
- C) LILO принципі бойынша жұмысістейтін құрылым
- D) LIFO принципі бойынша жұмыс істейтін құрылым
- **✅ E) дерек пен келесі түйінге көрсеткіші бар мәліметтер құрылымы**

**Түсіндірме:** Бір байланыстырылған тізімде әр түйін деректен және **тек келесі** түйінге сілтемеден тұрады. Алдыңғы мен келесіге сілтеме — қос байланыстырылған тізім.

---

### Сұрақ 20 (1-бөлім №20). C++ тілінде динамикалық бүтін типті массивті жариялау
- **✅ A) int* a = new int[n]**
- B) int* a = new int*[n]
- C) int a = new int[n]
- D) int a[n]
- E) int* a = new int(n)

**Түсіндірме:** Динамикалық массив: `int* a = new int[n];` — n элементті бүтін массив. `new int(n)` — мәні n болатын бір элемент, `new int*[n]` — көрсеткіштер массиві.

---

### Сұрақ 21 (1-бөлім №21). Стек әдістеріне жатпайтын функция
- A) pop()
- B) size()
- C) push()
- D) clear
- **✅ E) contains()**

**Түсіндірме:** Стектің негізгі операциялары — `push()`, `pop()`, `top()`, `size()`, `empty()`. `contains()` (элементті іздеу) стек интерфейсіне жатпайды.

**⚠ Тесттің кемшілігі:** сұрақ нақты бағдарламалау тілін көрсетпейді, ал жауап соған тәуелді:
- **C++ `std::stack`** — интерфейсте `push/pop/top/size/empty/emplace/swap` ғана бар, яғни `clear` **те** жоқ → D нұсқасы да формалды дұрыс.
- **Java `java.util.Stack`** — `Vector`-дан мұраланған `contains()` **те**, `clear()` **те** бар → екеуі де стек әдісі болып шығады.

Курстың контекстінде (стектің абстрактілі анықтамасы) күтілетін жауап — **contains()**, өйткені іздеу стектің ЛIFO семантикасына мүлдем жат.

---

### Сұрақ 22 (1-бөлім №22). Хеш кестесінің анықтамасы
- A) типтері бірдей элементтерден тұратын құрылым
- B) әр түрлі типті реттелген деректер құрылымы
- C) FIFO принципі бойынша жұмыс істейтін құрылым
- **✅ D) өте жылдам іздеу мен қоюды қамтамасыздандыратын деректер құрылымы**
- E) бір-бірімен байланысқан деректер құрылымы

**Түсіндірме:** Хеш-кесте кілт → индекс есептеу арқылы орташа **O(1)** уақытта жылдам іздеу мен қоюды қамтамасыз етеді.

---

### Сұрақ 23 (1-бөлім №23). Толтырылған массив ұяшығына кілтті хештеудің атауы
- A) бірігу
- **✅ B) коллизия**
- C) ажырау
- D) қиылысу
- E) бөліну

**Түсіндірме:** Екі түрлі кілт бір ұяшыққа хештелсе — бұл **коллизия**.

---

### Сұрақ 24 (1-бөлім №24). Хеш кестесінде коллизиядан құтылу әдісі
- **✅ A) ашық адресациялау әдісі**
- B) трапеция әдісі
- C) орамдар әдісі
- D) қадамдық бөлшектеу әдісі
- E) квадрат ортасы әдісі

**Түсіндірме:** Коллизияны шешу әдістері — **ашық адресациялау** (open addressing) және тізбектеу (chaining).

---

### Сұрақ 25 (1-бөлім №25). AVL ағашында жою операциясының орындалу уақыты
- A) O(n³)
- B) O(n²)
- C) O(n·log n)
- **✅ D) O(log n)**
- E) O(1)

**Түсіндірме:** AVL — теңгерілген ағаш, биіктігі O(log n). Іздеу, қою, жою — барлығы **O(log n)**.

---

### Сұрақ 26 (1-бөлім №26). 20 түйінді толық екілік ағаш, тамыры 0-деңгейде — 4-деңгейдегі түйін саны
- A) 0
- B) 8
- C) 1
- D) 4
- **✅ E) 5**

**Түсіндірме:** Деңгейлердегі түйін саны: 0→1, 1→2, 2→4, 3→8. Барлығы 15 түйін. Қалғаны `20−15 = 5` түйін 4-деңгейде орналасады.

---

### Сұрақ 27 (1-бөлім №27). Қызылды-қаралы ағаш қасиеттерінің бірі
- A) сол ішкі ағаштың кілті түйін кілтінен үлкен болса
- B) барлық жапырақтар бір деңгейде орналасса
- C) сол ішкі ағаштың кілті түйін кілтінен кіші болса
- **✅ D) егер төбе қызыл болса, оның екі мұрагері де қара**
- E) әрбір түйіннің ең болмағанда бір ішкі ағашы бос

**Түсіндірме:** Қызылды-қаралы ағаштың негізгі қасиеті: **қызыл түйіннің балалары міндетті түрде қара**. C — жалпы екілік іздеу ағашының қасиеті, қызылды-қаралыға тән емес.

---

### Сұрақ 28 (1-бөлім №28). Графтың түйінінің атауы
- A) мекенжай
- B) қабырға
- **✅ C) төбе**
- D) нүкте
- E) жол

**Түсіндірме:** Граф түйіні = **төбе (vertex)**. Түйіндерді қосатын — қабырға (edge).

---

### Сұрақ 29 (1-бөлім №29). Барлық төбелік жұптар үшін ең қысқа жолдарды құру есебі
- A) бастапқы төбеден бірнеше қабырға қашықтықтағы төбелерге дейін
- **✅ B) әр төбеден барлық басқа төбелерге дейін**
- C) бастапқы төбеден бір қабырға қашықтықтағы төбелерге дейін
- D) бастапқы төбеден басқа төбелерге дейін
- E) әр төбеден бастапқы төбеге дейін

**Түсіндірме:** «Барлық жұптар» есебінде (all-pairs shortest path, мыс. Флойд–Уоршелл) **әр төбеден барлық басқа төбелерге дейінгі** ең қысқа жол ізделеді.

---

### Сұрақ 30 (1-бөлім №30). Әр төбесі басқаларымен қабырғалармен жалғанған граф
- A) толық емес граф
- B) гиперграф
- C) шынжыр (тізбек)
- **✅ D) толық граф**
- E) мультиграф

**Түсіндірме:** Әр төбе қалған барлық төбелермен қабырғамен жалғанса — бұл **толық граф (complete graph)**.

---

## 2-СПЕЦ ПӘН. Бөлім: Деректер базасы (31–50)

> *Бір немесе бірнеше дұрыс жауабы бар (үш дұрыс жауаптан артық емес).*
> Сондықтан интерактивті нұсқада бұл бөлімнің **барлық** сұрағы checkbox-пен беріледі.

### Сұрақ 31 (2-бөлім №1). Факультет – Кафедра – Оқытушы байланысын бейнелейтін ағаштар тізімінен тұратын деректер моделі
- A) объектіге бағытталған
- **✅ B) иерархиялық**
- C) кестелік
- D) ER
- E) желілік
- F) реляциондық

**Түсіндірме:** Ағаш түріндегі (ата-ана → бала) байланыс — **иерархиялық** деректер моделі.

---

### Сұрақ 32 (2-бөлім №2). Біріктіру (union) амалының нәтижесі
- A) екі қатынасқа да кіретін кортеждер жиыны
- **✅ B) бірінші және екінші қатынасқа кіретін барлық кортеждер жиыны**
- C) бірінші қатынасқа кіретін, бірақ екіншісіне кірмейтін кортеждер жиыны
- D) берілген шартты қанағаттандыратын кортеждер жиыны
- E) берілген шартты қанағаттандырмайтын кортеждер жиыны
- **✅ F) кемінде бір қатынастағы барлық кортеждер жиыны**

**Түсіндірме:** Біріктіру (∪) — екі қатынастың **кемінде біреуінде** кездесетін барлық кортеждерді береді. A — қиылысу (∩), C — айырма (−).

**⚠ Тесттің кемшілігі:** B нұсқасының тұжырымы («бірінші **және** екінші қатынасқа кіретін») сөзбе-сөз оқығанда қиылысуды сипаттайды, яғни A-ны қайталайды. Бірмәнді дұрыс нұсқа — тек **F**. B «екі қатынастың да кортеждері біріктіріледі» деген мағынада жазылған деп есептеліп қосылды. Егер тек F-ті белгілесеңіз, интерактивті нұсқа ішінара ұпай (0.5) береді.

---

### Сұрақ 33 (2-бөлім №3). Жиыннан элементтерді тек берілген шарт бойынша іріктеп алу амалы
- A) айырмашылық
- B) Декарт көбейтіндісі
- C) проекция
- D) INTERSECT
- **✅ E) таңдау**
- F) бөлу
- G) қарым-қатынастарды біріктіру
- H) біріктіру

**Түсіндірме:** **Таңдау (selection, σ)** — шартқа сәйкес жолдарды (кортеждерді) іріктейді. Проекция — бағандарды таңдайды.

---

### Сұрақ 34 (2-бөлім №4). Жалғыз мәнді атрибут
- A) білім алу дипломы
- **✅ B) адам жасы**
- C) тауарлардың тізімі
- D) телефон номері
- **✅ E) факультет деканы**
- F) студенттер тізімі

**Түсіндірме:** Жалғыз мәнді (single-valued) атрибуттың бір ғана мәні болады: адамның жасы — бір сан, факультеттің деканы — бір адам. C және F — тікелей тізім, сондықтан көп мәнді.

**⚠ Ескерту:** A (диплом) мен D (телефон номері) сөзбе-сөз жекеше тұр, бірақ классикалық ДББ теориясында бұл екеуі көп мәнді атрибуттың хрестоматиялық мысалы — бір адамда бірнеше диплом да, бірнеше телефон да болуы мүмкін. Сондықтан оларды дұрыс жауапқа қоспаймыз.

---

### Сұрақ 35 (2-бөлім №5). Сыртқы кілтті құру шарты
- **✅ A) байланысқан кестенің бастапқы кілтінің типіне сай болу**
- B) кестелерді байланыстыруда қолданылмайды
- C) қатынастарды бірегей анықтауға мүмкіндік береді
- D) қатынастарды белгілі формаға келтіруде қолданылады
- E) байланысқан кестедегі NULL мәндерінен тұрады
- **✅ F) байланысқан кестенің әлеуетті кілтіне тәуелді болады**

**Түсіндірме:** Сыртқы кілт байланысқан кестенің бастапқы/әлеуетті кілтіне сілтейді және онымен **типі бойынша сәйкес** болуы керек.

---

### Сұрақ 36 (2-бөлім №6). Кітаптың аты мен авторлар тізімі
- **✅ A)**
  ```sql
  SELECT B.Title, A.Author
  FROM BOOKS AS B, UNNEST(B.AuthArray) AS A(Author);
  ```
- B)
  ```sql
  SELECT B.Title
  FROM BOOKS AS Title, UNNEST(B.AuthArray) AS A(Title);
  ```
- C)
  ```sql
  SELECT B.Title, A.Author
  FROM BOOKS AS B, NOT UNNEST(B.AuthArray) AS A(Author);
  ```
- D)
  ```sql
  SELECT B.Title, A.Author
  FROM BOOKS AS B, BOOKS AS A(Author);
  ```
- E)
  ```sql
  SELECT B.Title
  FROM BOOKS AS B;
  ```
- F)
  ```sql
  SELECT B.Title, A.Author
  FROM BOOKS AS B;
  ```

**Түсіндірме:** `UNNEST` — массивті (`AuthArray`) жеке жолдарға жаяды, әр кітап–автор жұбын шығарады. B-да бүркеншік аттар шатастырылған, C-да `NOT UNNEST` деген конструкция жоқ, D-да `BOOKS` өзімен қосылған, E мен F-та авторлардың көзі мүлдем жоқ.

---

### Сұрақ 37 (2-бөлім №7). Биология кафедрасының оқытушылары
- A)
  ```sql
  SELECT *
  FROM INSTRUCTOR
  WHERE DeptName <> 'Биология';
  ```
- B)
  ```sql
  SELECT *
  FROM INSTRUCTOR='Биология';
  ```
- C)
  ```sql
  SELECT Name
  FROM INSTRUCTOR
  GROUP BY DeptName;
  ```
- **✅ D)**
  ```sql
  SELECT Name
  FROM INSTRUCTOR
  WHERE DeptName = 'Биология';
  ```
- E)
  ```sql
  SELECT Name
  FROM INSTRUCTOR
  ```
- F)
  ```sql
  SELECT Name
  FROM INSTRUCTOR
  GROUP BY DeptName = 'Биология';
  ```

**Түсіндірме:** Дұрыс сүзгі — `WHERE DeptName = 'Биология'`. A-да `<>` (тең емес) — керісінше нәтиже, B — синтаксис қатесі, C/F — `GROUP BY` сүзгі емес, E — сүзгісіз барлық оқытушы.

---

### Сұрақ 38 (2-бөлім №8). «LIKE 'ab\%cd%' » түрінде берілген болса
- A) abcd тіркесі жоқ жазбалардан тұрады
- B) "ab"-мен басталып, "cd"-мен аяқталады
- C) "ab\%cd" деген тіркесті қарастырады
- **✅ D) "ab%cd" тіркесінен басталады**
- E) "abcd" тіркесінен басталады
- F) "abcd" тіркесімен аяқталады

**Түсіндірме:** `\%` — экрандалған, әдеттегі **%** таңбасы (литерал). Соңғы `%` — кез келген символдар. Демек жол **"ab%cd"** тіркесінен басталады.

**⚠ Ескерту:** кері қиғаш сызық экрандау таңбасы ретінде **MySQL**-де әдепкі бойынша жұмыс істейді. SQL стандартында және PostgreSQL-де ол үшін `ESCAPE '\'` деп нақты көрсету керек — онсыз `\` жай ғана литерал символ болып саналады.

---

### Сұрақ 39 (2-бөлім №9). Бөлім бойынша, әр бөлімнің ішінде жұмысшы фамилиясы, аты алфавит бойынша сұрыпталатын және жұмыс істейтін проектісі
- **✅ A)**
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
  WHERE D.Dnum = E.Dno AND E.Ssn = W.Essn AND W.Pno = P.Pnum
  ORDER BY D.Dname, E.Lname, E.Fname;
  ```
- B)
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
  WHERE D.Dnum = E.Dno OR E.Ssn = W.Essn OR W.Pno = P.Pnum;
  ```
- C)
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P;
  ```
- D)
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
  WHERE D.Dnum == E.Dno AND E.Ssn == W.Essn AND W.Pno == P.Pnum
  GROUP BY D.Dname, E.Lname, E.Fname;
  ```
- E)
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
  WHERE D.Dnum = E.Dno AND E.Ssn = W.Essn AND W.Pno = P.Pnum;
  ```
- F)
  ```sql
  SELECT D.Dname, E.Lname, E.Fname, P.Pname
  FROM DEPART AS D, EMPL AS E, WORKS_ON AS W, PROJECT AS P
  ORDER BY D.Dname, E.Lname, E.Fname;
  ```

**Түсіндірме:** Кестелерді дұрыс байланыстыратын `WHERE` шарттары **және** сұрыптау үшін `ORDER BY` бар жалғыз нұсқа — A. E-де `ORDER BY` жоқ, F-та байланыс шарты жоқ (Декарт көбейтіндісі шығады), B-да `AND` орнына `OR`, C-да екеуі де жоқ, D-да SQL-де жоқ `==` операторы және `ORDER BY` орнына `GROUP BY`.

---

### Сұрақ 40 (2-бөлім №10). Келесі сұраныстың нәтижесі
```sql
SELECT Semester, COUNT(*) AS '60-тан көп сағаттар саны'
FROM UCHPLAN
WHERE Hours>60
GROUP BY ALL Semester
```
- A) Әр семестрдегі сабақтар санының орташа мәнін шығарады
- B) Әр семестрдегі сағат саны 60 тан аз болатын сабақтар санын шығарады
- C) Барлық семестрлердегі сағат саны 60-тан көп сағаттар саны шығарады
- **✅ D) Әр семестрдегі сағат саны 60-тан көп болатын сабақтар санын шығарады**
- E) Әр семестрдегі сағат саны 60 қа тең болатын сабақтар санын шығарады
- F) Әр семестрдегі сабақтар санын және олардың атауларын шығарады

**Түсіндірме:** `WHERE Hours>60` сүзгісі + семестр бойынша топтау → әр семестр бойынша сағаты 60-тан асатын сабақтардың саны.

**⚠ Тесттің кемшілігі:** `GROUP BY ALL` — T-SQL кеңейтімі, ол `WHERE`-мен сүзіліп қалған семестрлерді де нәтижеге қосады (олардың `COUNT` мәні 0 болады). D нұсқасы кәдімгі `GROUP BY`-дың мінез-құлқын сипаттайды; `ALL`-дың дәл әсерін бірде-бір нұсқа сипаттамайды. Сондықтан D — қалған нұсқалардың ішіндегі жалғыз жарамдысы.

---

### Сұрақ 41 (2-бөлім №11). Әр проект үшін проект номерін, атын және сол проектте істеген жұмыскерлер саны
- **✅ A)**
  ```sql
  SELECT Pnum, Pname, COUNT(*)
  FROM PROJECT, WORKS_ON
  WHERE Pnum=Pno
  ```
- B)
  ```sql
  SELECT Pnum, Pname, COUNT(*)
  FROM PROJECT, WORKS_ON
  WHERE Pnum=Pno
  HAVING COUNT (*) > 2
  ```
- C)
  ```sql
  SELECT Pnum, Pname, COUNT(*)
  FROM PROJECT, WORKS_ON
  WHERE Pnum==Pno
  ```
- D)
  ```sql
  SELECT Pnum, Pname, COUNT
  FROM PROJECT, WORKS_ON
  WHERE Pnum==Pno
  ```
- E)
  ```sql
  SELECT Pnum, Pname, COUNT(Employers)
  FROM PROJECT, WORKS_ON
  WHERE Pnum=Pno
  ```
- F)
  ```sql
  SELECT Pnumber, Pname, COUNT (*)
  FROM PROJECT, WORKS_ON, EMPLOYEE
  WHERE Pnumber = Pno AND Ssn = Essn AND Dno = 5
  ```

**Түсіндірме:** Проект пен жұмысшыларды `Pnum=Pno` арқылы байланыстырып, `COUNT(*)` санайды. B артық `HAVING` шектеуін қосады, C/D-да SQL-де жоқ `==` операторы (әрі D-да `COUNT` жақшасыз), E-де жоқ баған, F-та артық `EMPLOYEE` қосылуы мен `Dno = 5` сүзгісі.

**⚠ Тесттің кемшілігі:** «Әр проект үшін» деген талап `GROUP BY Pnum, Pname` дегенді міндет етеді, бірақ **бірде-бір нұсқада `GROUP BY` жоқ** — яғни ешқайсысы толық дұрыс емес. A — қалғандарын шығарып тастау арқылы таңдалған жалғыз жарамды нұсқа.

---

### Сұрақ 42 (2-бөлім №12). Кесте құру барысында осы кілттік сөз арқылы негізгі кестедегі жол өшірілсе, онда байланысқан кестедегі жол да жойылады
- A) ON UPDATE CASCADE
- B) FOREIGN KEY
- C) ON UPDATE NO ACTION
- D) DELETE
- E) CHECK
- F) ON DELETE NO ACTION
- G) PRIVATE KEY
- **✅ H) ON DELETE CASCADE**

**Түсіндірме:** `ON DELETE CASCADE` — ата-жол жойылғанда, оған тәуелді жолдар автоматты түрде өшіріледі. `ON DELETE NO ACTION` — керісінше, жоюға тыйым салады.

---

### Сұрақ 43 (2-бөлім №13). Студенттер кестесінде стипендия мәні 25000 болатындарды екі есеге көбейту
- A)
  ```sql
  CREATE STUDENTS
  SET STIP = STIP * 2
  WHERE STIP = 25000;
  ```
- B)
  ```sql
  INSERT INTO STUDENTS
  SET STIP = STIP * 2
  WHERE STIP = 25000;
  ```
- C)
  ```sql
  DELETE STUDENTS
  WHERE STIP = 25000;
  ```
- D)
  ```sql
  INSERT STUDENTS
  SET STIP = STIP * 2
  WHERE STIP = 25000;
  ```
- **✅ E)**
  ```sql
  UPDATE STUDENTS
  SET STIP = STIP * 2
  WHERE STIP = 25000;
  ```
- F)
  ```sql
  UPDATE STUDENTS
  SELECT STIP = STIP *2
  WHERE STIP = 25000;
  ```

**Түсіндірме:** Бар жолдардың мәнін өзгерту — `UPDATE ... SET ... WHERE`. `CREATE` кесте жасайды, `INSERT` жаңа жол қосады, `DELETE` жолды өшіреді, F-та `SET` орнына `SELECT` жазылған.

---

### Сұрақ 44 (2-бөлім №14). 2003 кодтағы сабақтан барлық студенттердің балын NULL-ға өзгерту
- A)
  ```sql
  INSERT USP
  SET Ocenka = NULL
  WHERE Pnum > 2003;
  ```
- B)
  ```sql
  CREATE USP
  SET Ocenka = NULL
  WHERE Pnum = 2003;
  ```
- C)
  ```sql
  INSERT INTO USP
  SET Ocenka = NULL
  WHERE Pnum = 2003;
  ```
- **✅ D)**
  ```sql
  UPDATE USP
  SET Ocenka = NULL
  WHERE Pnum = 2003;
  ```
- E)
  ```sql
  INSERT INTO USP
  SET Ocenka = NULL;
  ```
- F)
  ```sql
  UPDATE USP
  SET Ocenka = NULL;
  ```

**Түсіндірме:** Мәнді NULL-ге өзгерту — `UPDATE USP SET Ocenka = NULL WHERE Pnum = 2003`. F-та `WHERE` жоқ — бүкіл кестенің бағасы өшеді, A-да `> 2003` (басқа жиын), қалғандарында дұрыс емес оператор.

---

### Сұрақ 45 (2-бөлім №15). UNIQUE кілттік сөзін қолданып, 2021 жылы бір рет қана оқылған пәндер
- A)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE UNIQUE (SELECT R.CourseId FROM SECTION AS R
                WHERE T.CourseId = R.CourseId OR R.Year = 2021);
  ```
- B)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE NOT UNIQUE (SELECT R.CourseId FROM SECTION AS R
                    WHERE T.CourseId = R.CourseId AND R.Year = 2021);
  ```
- C)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE SOME (SELECT R.CourseId FROM SECTION AS R
              WHERE T.CourseId = R.CourseId AND R.Year = 2021);
  ```
- **✅ D)**
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE UNIQUE (SELECT R.CourseId FROM SECTION AS R
                WHERE T.CourseId = R.CourseId AND R.Year = 2021);
  ```
- E)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE UNIQUE (SELECT CourseId FROM SECTION AS R
                WHERE CourseId = CourseId AND Year = 2021);
  ```
- F)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE UNION(SELECT R.CourseId FROM SECTION AS R
              WHERE T.CourseId = R.CourseId AND R.Year = 2021);
  ```

**Түсіндірме:** `UNIQUE` предикаты ішкі сұраныс нәтижесінде қайталанатын жол болмаса `true` қайтарады — яғни пән 2021 жылы бір-ақ рет оқылған. Корреляция (`T.CourseId = R.CourseId`) мен жыл сүзгісі (`R.Year = 2021`) `AND` арқылы дұрыс қосылған жалғыз нұсқа — D. A-да `OR`, B-да `NOT UNIQUE` (керісінше нәтиже), C-да `SOME`, E-де корреляция жалған (`CourseId = CourseId`), F-та `UNION` предикат емес.

---

### Сұрақ 46 (2-бөлім №16). 2021 жылы бір рет қана оқылған пәндер
- **✅ A)**
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE T.CourseId = R.CourseId AND R.Year = 2021);
  ```
- B)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE T.CourseId = R.CourseId OR
              R.Year = 2021);
  ```
- C)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE T.CourseId = R.CourseId);
  ```
- D)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE T.CourseId = R.CourseId OR R.Year = 2021);
  ```
- E)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE CourseId = CourseId AND Year = 2021);
  ```
- F)
  ```sql
  SELECT T.CourseId FROM COURSE AS T
  WHERE 1 <= (SELECT COUNT(R.CourseId) FROM SECTION AS R
              WHERE R.Year = 2021);
  ```

**Түсіндірме:** Ішкі сұраныста корреляция да (`T.CourseId = R.CourseId`), жыл сүзгісі де (`R.Year = 2021`) `AND` арқылы дұрыс орналасқан жалғыз нұсқа — A. C-да жыл жоқ, F-та курс корреляциясы жоқ, E-де корреляция жалған, B/D-да `AND` орнына `OR`.

**⚠ Тесттің кемшілігі:** сұрақта «бір рет **қана**» делінген, бұл `1 = COUNT(...)` дегенді талап етеді. Ал алты нұсқаның бәрінде `1 <= COUNT(...)`, яғни «кемінде бір рет» жазылған — ешқайсысы сұрақтың шартын дәл өрнектемейді. Нұсқалар тек ішкі сұраныстың шарты бойынша ажыратылады.

---

### Сұрақ 47 (2-бөлім №17). Философия пәнінің кодынан 1 санға кем сабақ туралы ақпарат
- **✅ A)**
  ```sql
  SELECT * FROM PREDMET
  WHERE Pnum = (SELECT Pnum-1 FROM PREDMET
                WHERE Pname = 'Философия');
  ```
- B)
  ```sql
  SELECT * FROM PREDMET
  WHERE Pnum >= (SELECT Pnum-1 FROM PREDMET;
                 WHERE Pname = 'Философия');
  ```
- C)
  ```sql
  SELECT * FROM PREDMET
  WHERE Pnum = Pnum-1
  ```
- D)
  ```sql
  SELECT * FROM PREDMET
  WHERE (SELECT Pnum-1 FROM PREDMET
         WHERE Pname = 'Философия');
  ```
- E)
  ```sql
  SELECT * FROM PREDMET
  WHERE Pnum = (SELECT Pnum-1FROM PREDMET);
  ```
- F)
  ```sql
  SELECT * FROM PREDMET
  WHERE Pnum <= (SELECT Pnum-1 FROM PREDMET
                 WHERE Pname = 'Философия');
  ```

**Түсіндірме:** Ішкі сұраныс Философияның кодынан 1 кемін (`Pnum-1`) есептейді, сыртқы сұраныс сол кодты жазбаны табады. B/F-та `>=` және `<=` — бір жазба емес, диапазон қайтарады (әрі B-да артық `;`), C-да ішкі сұраныс мүлдем жоқ, D-да салыстыру операторы жоқ, E-де синтаксис қатесі (`Pnum-1FROM`) және пән аты бойынша шарт жоқ.

---

### Сұрақ 48 (2-бөлім №18). 1НФ
- A) кілттік емес атрибуттары толығымен бастапқы кілтке тәуелді қатынас
- B) кілттік емес атрибуттар өзара тәуелді емес қатынас
- **✅ C) логикалық бөлінбейтін мәндерге ие қатынас**
- D) кестелер бір кестеге біріктірілетін қатынас
- E) тұтастық сақталмайтын қатынас
- F) логикалық бөлінетін мәндерге ие қатынас

**Түсіндірме:** 1НФ — барлық атрибут мәндері **атомарлы (әрі қарай логикалық бөлінбейтін)** болуы шарт. A — 2НФ, B — 3НФ анықтамасы.

---

### Сұрақ 49 (2-бөлім №19). Скалярлық қатынас
- A) сыртқы кілттік емес атрибуттары толығымен бастапқы кілтке тәуелді
- B) кілттік емес атрибуттары толығымен бастапқы кілтке тәуелді
- C) кестелер бір кестеге біріктіріледі
- **✅ D) логикалық бөлінбейтін мәндерге ие қатынас**
- E) логикалық бөлінетін мәндерге ие
- F) логикалық емес бөлінбейтін мәндерге ие қатынас

**Түсіндірме:** Скалярлық (атомарлы) қатынас — әр ұяшықта бір ғана **бөлінбейтін** мән болады.

**⚠ Ескерту:** бұл нұсқаның мәтіні 48-сұрақтағы (1НФ) дұрыс жауаппен сөзбе-сөз бірдей — тестте скалярлық қатынас пен 1НФ бір ғана анықтама арқылы берілген.

---

### Сұрақ 50 (2-бөлім №20). Денормалауда пайда болатын аномалия
- A) іздеу аномалиясы
- **✅ B) өзгерту аномалиясы**
- C) қосылу аномалиясы
- **✅ D) енгізу аномалиясы**
- E) сұрыптау аномалиясы
- **✅ F) жою аномалиясы**
- G) байланыс аномалиясы

**Түсіндірме:** Классикалық үш аномалия: **енгізу (insert)**, **өзгерту/жаңарту (update)**, **жою (delete)**. Іздеу, сұрыптау, қосылу және байланыс аномалиясы деген ұғымдар жоқ.

---

## Ұпай санау туралы

Тест бойынша барлығы **70 ұпай**: 1-бөлім 30 × 1 = 30, 2-бөлім 20 × 2 = 40.

**Ұпай әрқашан бүтін — 0, 1 немесе 2. Бөлшек ұпай жоқ.**

- **1-бөлім** (1 ұпай): дұрыс нұсқа — 1, қате не бос — 0.
- **2-бөлім** (2 ұпай):
  - барлық дұрыс нұсқа белгіленсе (артығы жоқ) — **2**;
  - дұрыс нұсқалардың бір бөлігі ғана белгіленсе — **1**;
  - ең болмағанда бір қате нұсқа белгіленсе — **0**.

Мұның салдары: кілтте бір ғана дұрыс жауабы бар 2-бөлім сұрағында сол нұсқаны белгілесеңіз
2 ұпай аласыз, ал «сақтық үшін» тағы біреуін қоссаңыз — 0 ұпай. Кездейсоқ бәрін белгілеп
ұпай жинау мүмкін емес.

---

*Ескерту: дұрыс жауаптар стандартты алгоритм/ДББ теориясы бойынша анықталды. ⚠ таңбасымен
белгіленген жерлерде қателік шешімде емес, тесттің өз тұжырымында.*
